
{  
  "name": "project-name",  
  "description": "",
  "main": "server.js",
  "version": "0.1.0",  
  "private": true,  
  "dependencies": {    
    "react": "^16.6.3",    
    "react-dom": "^16.6.3",    
    "react-scripts": "2.1.1",    
    "react-router-dom": "^4.3.1"  ,
    "concurrently": "^5.3.0",
    "express": "^4.17.1"
  },
  "scripts": {    
    "client-install": "npm install --prefix calculator",    
    "start": "node server.js",    
    "server": "nodemon server.js",    
    "client": "npm start --prefix calculator",    
    "dev": "concurrently \"npm run server\" \"npm run calculator\""
  },
  

  "eslintConfig": {    "extends": "react-app"  },
  "proxy": "http://localhost:5000",
  "browserslist": [    ">0.2%",    "not dead",    "not ie <= 11",    "not op_mini all"  ]
}

